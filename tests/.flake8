[flake8]
# `django-admin startproject` generates some files with lines longer than 80
# characters...let's just let it do that.
max-line-length = 100
ignore =
    # E741: We use "l" as a variable in listcomps, this is fine.
    E741
